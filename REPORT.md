  ## PART 1: ( What & why):
- CineBot is a friendly conversational AI Powered Web App Chatbot that provides users with their own personalized movie recommendations! Instead of being a generic search-engine like Chatbot that only takes a single line of input and returning a list of generalized movies, CineBot will engage with users in nantural multi-turn conversations. CineBot will ask users about their moods, preferred movie genre, and streaming platform, before it gives the user a movie recommendation with an explanation. To put it simply, the app incorperates the OpenAI chat completions API with a custom system prompt that will guide the behavior of CineBot across entire conversations with the user. The conversation history is saved and designed to remember previous user inputs. The convo history is handled in the frontend with JavaScript as an array and sent with every request, so that CineBot has full history context across a conversation. 
- The target audience: is people who enjoy movies who tend to open any movie streaming platform, stare at the menu screen for 15 minutes, and close it without watching anything due to struggling to pick a film they think is worth their time!
- The hardest part about getting the AI behavior right would be getting the AI model to properly understand when it has collected enough information as direted in the system prompt to make a movie recommendation rather than going in a loop of asking questions. For this project, it was pretty difficult for me to decide the right 'fit' regarding exactly how 'conversational' I wanted the CineBot to be, as it left me in a crossroad of a problem tradeoff: I could allow CineBot to be more conversational before instructing it to make a recommendation, however this would introduce problems for the evaluation aspect. It would be very difficult to grade the evaluation, as I would have to define what persnalization would be, evaluate conversational quality, deep reasoning, etc,  which would be very hard to define or grade. However, I ended going in the middle, where I avoided overcorrecting the bot/making it afraid to ask follow up questions where needed, (such as if the user wanted a different movie suggestion), while also adding a sufficient limit. This helped me solve the issue of having an eval that was inconsistent or inaccurate, and allowed me to easier evaluate the model, and have the model provide recommendations that are more determinisitic and consistent. 

## PART 2: (Iterations (≥3 versions, ~75–150 words each):
-- VERSION 1. BASELINE --
Change: Thinking of this as going from 0 to 1, I implemented my main system prompts that provided the foundatiion for how the CineBot AI model will behave in the appT. My prompt implements 4 rules: Ask at least one clarifying question before recommending a movie,       always include a movie title, genre, reasoning, and streaming platform in every recommendation, maintain a friendly conversational tone, and ask follow up questions instead of just dumping a list if the user doesn't like the recommendation.

Motivating example:A motivating example that led me to think v1 would be a good start place for my prompts would be a scenario where a user tells the AI: "I want to watch a hilarious movie tonight", and then the bot would just provide the user with seemingly random movie reccomendations that are not personalzied, or provide any garuantee of listed eval metrics I defined such as always including a streaming platform, follow up questions, etc. 

Delta: The metric is the baseline with no before and after comparison. It has an initial eval score of 68.4%. Meaning 13 out of 19 test cases passed in the eval.

Conclusion: Baseline, metric is neutral and I'm unsure how good or bad the model performs. 



-- VERSION 2. --
Change: I carefully constructed and implemented an additional new rule (Rule #5) into the system prompts for both 'app.py' and 'eval.py'. This rule states "As soon as you have enough information to make a relevant recommendation (genre/mood and streaming platform), immediately provide a recommendation and avoid asking unecessary follow-up questions. 

Motivating example: Though there were serveral failing cases (5 total) that motivated this change, Test Case 03 motivated this change the most as it was the most obvious. Essentially, it was most clear that by adding this change, it would most likely fix this test case fail and likely the other failing test cases too. In tc03, we can clearly see that the user had already provided the AI chatbot with a desired genre/mood (classic romance), a streaming platform (HBO Max),and also a response to at least one clarifying follow up question,  yet the AI still asked an additional uncessary follow up question ("Just to narrow it down a bit more, are you in the mood for something more light-hearted or something a bit more dramatic?"). The AI asked this uneeded question instead of recommending a movie to the user, despite already having every metric it needed to provide a recommendation. 

Delta: Metric before: 68.4% (13/19). Metric After: → 94.7% (18/19)! Huge positive improvement of +26% increase in accuracy on the evaluation!

Conclusion: After performing the system prompt changes, the metric significantly improved because 5 out of the 6 total failures were all being caused by the same core issue, in which the AI model (CineBot) would keep asking additional unecessary follow up questions despite already having the information it needed from the user to recommend a movie. By adding the new additional rule #5 to the system prompt, it pushed CineBot to immediately provide the user with a movie recommendaition as soon as it collected enough prefrence information from the user, and made it a clear stopping point expectation in the sytsem prompt. This prompt change directly fixed and turned those 5/6 failing cases into passes, as the expected output (movie recommendation with the user's preferences) was now satisified. For my next eval, I'm aiming to fix the last error, test case 013. The error appears to be caused by the AI model still asking an additional unecessary follow up question but for a different reason in this scenario.



-- VERSION 3. --
Change: I crafted a careful extension to the end Rule #5 in the system prompts for both 'app.py' and 'eval.py' The extension states, "*Even if the user provides super simple short sentences, this may still count as enough relevant information for you to make a movie recommendation, given that the user provides you with the genre/mood and streaming platform." 

Motivating example: Test case 13 was the only case that failed from my eval set, so this ultimately motivated the change. In tc13 the user starts the convo by asking CineBot to recommend them a movie.  CineBot then asks the user "What kind of mood are you in — comedy, drama, thriller, something else? And what streaming platform do you have?" The user then responded with the following: "I'm in the mood for a thriller. I have Netflix." In this case, even though the user had already provided CineBot with enough the needed information to make a recommendation, the AI still asked more unnecessary follow up questions, ("How about a psychological thriller? It can really keep you on the edge of your seat. Would you prefer something more recent or a classic?") rather than recommending them a movie and only giving follow up questions in the event that the user requests a different movie. 

Delta: Metric before: 94.7% (18/19). Metric After: → 100% (19/19)! Great improvement of +5.3%! 

Conclusion: After adding the system prompt changes, the metric moved up to 100% accuracy rate in the eval for all 19 test cases! The metric increased because before adding the prompt extension to rule #5, the model may have likely viewed shorter replies from the user such as the one seen in tc13 as insufficent amount of information. Because the system prompt specifically tells CineBot to immediately give a recommendation only AFTER it receives enough information, CineBot may have been reluctant to give a recomendation because shorter sentences may read as potentially 'not enough' information to it. By directly telling CineBot that short simple responses from the user may still be viewed as a sufficent response (given that the proper eval metrics are still provided), it gives CineBot the confidence and reassurance it may need to make a recommendation as soon as it has enough information from the user, despite a minimal response from the user. 



## PART 3: (Code walkthrough):
- One user action for my app, would be the action where the user requests CineBot to provide them with a movie recommendation, based on preferences they desire. The user does this by launching the app, and entering a message directly into the input box. As soon as the message is entered, the request will then be processed through the frontend chat UI. The function 'handleSend()' is then called, this is located in static/index.html:305. The function then reads the users text input and appends it into the apps chat window, using the function 'appendMessage', then instantly disabling the send message button in order to prevent the user from sending multiple messages simultaneously. It then calls the function fetch("/chat", located in index.html:318, which sends a JSON POST request that contains 2 fields: the current 'message' of the user, and a full 'history' array, which contains the previous user history conversation up until this point for full context. Now, regarding the backend of the app, located in 'app.py', the '/chat' route in app.py:45 will recieve this reuqest. It will then extract 'user_message', located in app.py:47, and 'history', located in app.py:48. It will then create the 'messages' array in app.py:53–55. It does this by prepending the system prompt, appending the full convo history for context, and lastly, appending the user's new message. The entire array is passed to 'client.chat.completions.create()', which  in app.py:58, which will send the full contextualized convo history to the ChatGPT OpenAI API system. The model's reply will be extracted and found app.py:64, then reutnred as JSON. Finally, the frontend will recieve the reply, and both the user's messsage and CineBots reply will be pushed into the 'history' array, located in index.html:329–330, which will keep the conversation state ready for the next turn(s) and so the user can view the recommendation. This could be potentially hazarous, as a user could pay a subscription fee for access to a certain platform, and then be very frustrated with the company if the movie is not actually available in that platform. 

- One design descision: would be using a Java Script array to store the entire conversation history, rather than using something much more complicated and prone to issues such as implementing a server side session or database implementation. By avodiing a need to depend on a database, it keeps the backend stateless, also  simplifying the process for the steup for a fresh grader clone by a lot.

- One rejected alternative would be using the session object from Flask as the method to store history server side. Initially I thought about using it, however I realized it would be a headache to deal with the state management complexity issues it could introduce, and 
also other issues that could arrise if the user opens many tabs, etc. 


## PART 4: (AI disclosure & safety):
I used Claude as my primary AI coding assistant throughout the development process for this project, such as assitance with creating the eval score_response run_eval functions, and a few test cases. I also used some use of ChatGPT for small portions of the project, such as recommending alternative idea approaches for how I could build this project, and most importantly, to generate half of the test casses so I can get a variety of them and avoid test case model bias, because as you've explained, it would be like asking a student to take their own test, not optimal. There were several instances where Claude failed, as such as initially recommending system prompts that were too complex to be able to reliably score in the eval function, such as by recommending a conversational quality metric, which would be too dificul to set a defined acceptable and non-acceptable value. As I mentioned in Part 1, I recovered by picking a middle ground, and picked eval metrics that allow some flexibility for its conversational abilities, while still avoiding overly complex metrics that would be too complex to reliably grae in the eval. Claude also failed with maintaing core system prompt eval metrics, as it would sometimes 'forget' to check for the movies 'platform' from the user, such as in the eval function. One major saftey risk in my app would be hallucination, specifically for the availability of streaming platforms for movies. The model tends to be very confident with providing platforms that a certain movie is available on, however sometimes it may hallucinate a platform availability, such as by saying that a movie is available on Disney+ when its actually only on Netflix. 
